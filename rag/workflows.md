# ⚙️ Workflows

## Development

* **Start**: `npm run dev` (Starts the Vite development server)
* **Build**: `npm run build` (Builds the app for production)
* **Preview**: `npm run serve` (Previews the built app)

## Deployment

* **Method**: Static file serving / Local execution.
